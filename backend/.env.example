# Required to activate the OpenAI Universal BIS Agent.
# Keep this value secret; never commit a real API key.
OPENAI_API_KEY=

# Optional. The agent defaults to gpt-5-mini for cost-conscious deployment.
OPENAI_AGENT_MODEL=gpt-5-mini
