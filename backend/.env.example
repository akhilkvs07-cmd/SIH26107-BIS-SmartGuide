# Free Gemini Developer API key for the Universal BIS Agent.
# Create one in Google AI Studio: https://aistudio.google.com/app/apikey
# Keep this value secret; never commit a real API key.
GEMINI_API_KEY=

# Optional. gemini-3.8-flash currently has a free API tier with model-specific rate limits.
GEMINI_AGENT_MODEL=gemini-3.8-flash
